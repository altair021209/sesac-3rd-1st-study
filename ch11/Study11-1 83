
D= [0] * 51
probability = [0] * 51
M = int(input())
D = list(map(int, input(). split()))
T =0
for i in range(O, M) :
  T+= D[i]
K= int(input ())
ans = 0
for i in range(0, M) :
  if D[1] >= K:
    probability[i] = 1
    for k in range(0, K) :
    probability[i] = probability[i] * (D[i] - k) / (T - k) #확률식
ans += probability[i]
