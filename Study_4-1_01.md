#숫자의 합 구하기

*문제: 공백없이 주어진 N개의 숫자를 모두 합해 출력하는 프로그램 작성
*핵심 아이디어: 주어진 숫자를, 인덱스를 가진 리스트로 인식하여, 인덱스의 요소를 모두 더해줌

*코드:

```python

N = input() #입력할숫자의 갯수
numbers = list(inputs()) #이대로 입력하면 숫자들이 문자로 인식되어 각각의 요소로 취급됨
sum = 0

for i in numbers:
    sum += int(i) # "i"눈 문자취급이기에 명시적 형변환이 필요

print(sum)


```