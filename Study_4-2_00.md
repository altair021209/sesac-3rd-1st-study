#구간 합

구간 합은 시간 복잡도를 더 줄이기 위해 사용하는 알고리즘

*합 배열
합 배열은 기존의 리스트 데이터를 전처리한 배열로, [0] 부터 [i] 까지의 합을 리슽의 [i] 인덱스에 저장한다. 합 배열을 S, 원본 배열을 A라 할 때 이하의 식으로 구성된다

S[i] = A[0] + A[1] + A[2] + ... A[i]

A[i] 부터 A[j]까지의 합을 합배열 없이 구하는 경우, 시간 복잡도는 O(N)이 된다

